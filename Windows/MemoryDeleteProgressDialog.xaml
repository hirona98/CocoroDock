<Window x:Class="CocoroDock.Windows.MemoryDeleteProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="記憶を削除しています..."
        Height="200"
        Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- メッセージ -->
        <TextBlock Grid.Row="0"
                   x:Name="MessageText"
                   Text="記憶を削除しています..."
                   FontSize="14"
                   Margin="0,0,0,10"/>

        <!-- キャラクター名 -->
        <TextBlock Grid.Row="1"
                   x:Name="DetailsText"
                   Text="キャラクター: -"
                   Foreground="#666666"
                   Margin="0,0,0,20"/>

        <!-- プログレスバー -->
        <ProgressBar Grid.Row="2"
                     x:Name="ProgressBar"
                     Height="20"
                     IsIndeterminate="True"/>

        <!-- 注意書き -->
        <TextBlock Grid.Row="3"
                   Text="処理中です。しばらくお待ちください..."
                   Foreground="#888888"
                   FontSize="11"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>